name: Scala build Docker image

on:
  workflow_call:
    inputs:
      registry:
        required: true
        type: string
      image-name:
        description: "Optional image name; if empty, derived from repo name (without brijyt- prefix)"
        required: false
        type: string
        default: ""
    outputs:
      artifact-name:
        description: "Name of the artifact containing the saved Docker image"
        value: ${{ jobs.build-image.outputs.artifact-name }}
      local-image:
        description: "Local reference of the built image"
        value: ${{ jobs.build-image.outputs.local-image }}

jobs:
  build-image:
    runs-on: ubuntu-latest
    outputs:
      artifact-name: ${{ steps.save.outputs.artifact_name }}
      local-image: ${{ steps.save.outputs.local_image }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.sha }}

      - name: Set image name
        id: set-image-name
        run: |
          if [ -n "${{ inputs.image-name }}" ]; then
            echo "name=${{ inputs.image-name }}" >> "$GITHUB_OUTPUT"
          else
            repo_name="${GITHUB_REPOSITORY#*/}"
            echo "name=${repo_name#brijyt-}" >> "$GITHUB_OUTPUT"
          fi

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Setup sbt
        uses: sbt/setup-sbt@v1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Set BuildInfo SHAs for Docker image
        run: |
          echo "BUILD_TESTED_SHA=${{ github.sha }}" >> "$GITHUB_ENV"
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "BUILD_SOURCE_SHA=${{ github.event.pull_request.head.sha }}" >> "$GITHUB_ENV"
          else
            echo "BUILD_SOURCE_SHA=${{ github.sha }}" >> "$GITHUB_ENV"
          fi

      - name: Build Docker image with sbt
        run: sbt docker:publishLocal
        env:
          SBT_OPTS: "-Dsbt.log.noformat=true"

      - name: Detect local image and save as artifact
        id: save
        shell: bash
        run: |
          set -euo pipefail
          LOCAL_IMAGE="$(docker images --format '{{.Repository}}:{{.Tag}}' | grep -E '${{ steps.set-image-name.outputs.name }}' | head -1)"
          if [[ -z "${LOCAL_IMAGE}" ]]; then
            echo "No local sbt image found." >&2
            exit 1
          fi
          echo "Local image: ${LOCAL_IMAGE}"
          docker save "${LOCAL_IMAGE}" -o image.tar
          echo "artifact_name=built-image" >> "$GITHUB_OUTPUT"
          echo "local_image=${LOCAL_IMAGE}" >> "$GITHUB_OUTPUT"

      - name: Upload image artifact
        uses: actions/upload-artifact@v4
        with:
          name: built-image
          path: image.tar
